zuul:
  sensitive-headers: 
  host:
    connect-timeout-millis: 5000
    socket-timeout-millis: 5000
  ignored-patterns:
    - /**/product/listForOrder
management:
  endpoints:
    web:
      exposure:
        include: "*"
