zuul:
  sensitive-headers: 
  host:
    connect-timeout-millis: 3000
    socket-timeout-millis: 3000
  routes:
    user: /user/**
    myProduct:
      path: /myProduct/**
      serviceId: product
  ignored-patterns:
    - /**/product/listForOrder
management:
  endpoints:
    web:
      exposure:
        include: "*"
